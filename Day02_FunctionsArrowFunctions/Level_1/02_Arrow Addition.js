// Write an arrow function add(a, b) that takes two numbers and returns their sum. Log the result of calling the function.

const add = (a,b) => a + b ;

let num1 = 3 ,num2 = 7;

console.log(`${num1} + ${num2} = ${add(num1,num2)}`);