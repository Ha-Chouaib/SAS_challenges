//Given a string, log its length, and then log the entire string in uppercase.

let lowerName = "chouaib hadadi";

let uperName = lowerName.toLocaleUpperCase();

console.log(lowerName);
console.log(uperName);
